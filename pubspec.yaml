name: tree_law_zoo
description: "Tree Law Zoo - Smart Restaurant Super App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  
  # Supabase
  supabase_flutter: ^2.3.0
  
  # State Management
  flutter_riverpod: ^2.4.0
  
  # Navigation
  go_router: ^13.0.0
  
  # UI
  cached_network_image: ^3.3.0
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  shimmer: ^3.0.0
  
  # Utils
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Assets (uncomment when files are added)
  # assets:
  #   - assets/images/
  #   - assets/icons/

  # Fonts - using Google Fonts package instead
  # fonts:
  #   - family: Sarabun
  #     fonts:
  #       - asset: assets/fonts/Sarabun-Regular.ttf
  #       - asset: assets/fonts/Sarabun-Medium.ttf
  #         weight: 500
  #       - asset: assets/fonts/Sarabun-SemiBold.ttf
  #         weight: 600
  #       - asset: assets/fonts/Sarabun-Bold.ttf
  #         weight: 700
